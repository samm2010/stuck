<%if activity.trackable%>
<p><%= link_to activity.owner.email, user_path(activity.owner.id) %> is now friends with <%= link_to activity.recipient.email, user_path(activity.recipient.id) %></p>
<% end %>